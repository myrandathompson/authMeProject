{
  "name": "authmeproject",
  "version": "1.0.0",
  "description": "## Database Schema Design",
  "main": "index.js",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build:backend": "cd backend && NODE_ENV=production npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start:backend": "cd backend && NODE_ENV=production pm2 start server.js --name authme-backend",
    "start:frontend": "cd frontend && serve -s build -l 3000",
    "start:all": "npm run start:backend && npm run start:frontend"
  },
  "dependencies": {
    "pm2": "^5.3.0",
    "serve": "^14.2.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
